# 🚀 Quick Claude Commands for LearnIQ

## 📱 Most Used Commands

### **Code Generation**
```
Cmd+L → "Generate a Flutter widget for [feature]"
Cmd+L → "Create a Riverpod provider for [state]"
Cmd+L → "Add error handling to this function"
Cmd+L → "Generate unit tests for this class"
```

### **Code Improvement**
```
Cmd+K → "Optimize this widget for performance"
Cmd+K → "Add accessibility features to this screen"
Cmd+K → "Improve the user experience of this feature"
Cmd+K → "Refactor this code to follow best practices"
```

### **Debugging**
```
Cmd+K → "Debug this issue: [paste error]"
Cmd+I → "Explain what this code does"
Cmd+K → "Suggest debugging strategies for this problem"
```

### **Localization**
```
Cmd+L → "Add localized strings for [feature]"
Cmd+K → "Review translations for consistency"
Cmd+L → "Update UI to support new languages"
```

## 🎯 Project-Specific Commands

### **Learning Features**
```
Cmd+K → "Design a new learning game for vocabulary"
Cmd+L → "Implement spaced repetition algorithm"
Cmd+K → "Add progress tracking to learning sessions"
Cmd+L → "Create engaging animations for card interactions"
```

### **Audio Features**
```
Cmd+L → "Improve TTS service with error handling"
Cmd+K → "Add audio controls to learning cards"
Cmd+L → "Handle audio permissions properly"
```

### **UI/UX**
```
Cmd+K → "Enhance the visual design of this screen"
Cmd+L → "Add smooth transitions and animations"
Cmd+K → "Improve navigation flow between screens"
Cmd+L → "Add loading states and error feedback"
```

## 🔧 Advanced Commands

### **Architecture**
```
Cmd+K → "Design architecture for [new feature]"
Cmd+K → "Analyze performance bottlenecks in this code"
Cmd+K → "Suggest improvements to state management"
```

### **Testing**
```
Cmd+L → "Generate comprehensive test suite"
Cmd+K → "Create integration tests for user flows"
Cmd+L → "Add widget tests for UI components"
```

### **Documentation**
```
Cmd+L → "Generate documentation for this code"
Cmd+K → "Create user guide for this feature"
Cmd+L → "Update README with new features"
```

## ⚡ Pro Tips

1. **Use `Cmd+K` for complex analysis and planning**
2. **Use `Cmd+L` for quick code generation**
3. **Use `Cmd+I` for inline help and explanations**
4. **Right-click for contextual menu options**
5. **Select multiple files for cross-file analysis**

## 🎨 Example Workflows

### **Adding a New Feature**
```
1. Cmd+K → "Plan the architecture for [feature]"
2. Cmd+L → "Generate the model classes"
3. Cmd+L → "Create the UI widgets"
4. Cmd+L → "Add state management"
5. Cmd+L → "Generate tests"
6. Cmd+K → "Review and optimize"
```

### **Fixing a Bug**
```
1. Cmd+K → "Analyze this error: [paste error]"
2. Cmd+I → "Add debug logging"
3. Cmd+L → "Fix the issue"
4. Cmd+L → "Add tests to prevent regression"
```

### **Code Review**
```
1. Cmd+K → "Review this code for best practices"
2. Cmd+L → "Suggest improvements"
3. Cmd+K → "Check for performance issues"
4. Cmd+L → "Add missing error handling"
```

## 📊 Performance Commands

```
Cmd+K → "Analyze this code for performance issues"
Cmd+L → "Optimize widget rebuilds"
Cmd+K → "Check for memory leaks"
Cmd+L → "Add performance monitoring"
```

## 🔍 Debugging Commands

```
Cmd+K → "Help me debug this Flutter issue"
Cmd+L → "Add comprehensive error handling"
Cmd+K → "Create a minimal reproduction case"
Cmd+L → "Add logging and debugging tools"
```

---

**Remember**: Claude Pro has larger context windows, so you can reference multiple files and get more comprehensive analysis!
